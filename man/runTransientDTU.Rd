% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runTransientDTU.R
\name{runTransientDTU}
\alias{runTransientDTU}
\title{Run the complete transient-DTU decision workflow}
\usage{
runTransientDTU(
  pairwise,
  stage_order,
  usage = NULL,
  col_data = NULL,
  replicate_action = NULL,
  incomplete_replicates = c("drop", "keep", "error"),
  panel_size = 6L,
  reciprocal_only = TRUE,
  unique_genes = TRUE,
  ...
)
}
\arguments{
\item{pairwise}{Generic pairwise DTU result table; see \code{\link[=makeStageDTU]{makeStageDTU()}}.}

\item{stage_order}{Complete biological stage order.}

\item{usage}{Optional feature-by-sample usage matrix or
\link[SummarizedExperiment:RangedSummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment}.}

\item{col_data}{Sample metadata required for matrix \code{usage}.}

\item{replicate_action}{\code{"none"}, \code{"annotate"}, or \code{"filter"}. If \code{NULL},
it defaults to \code{"filter"} when \code{usage} is supplied and \code{"none"}
otherwise.}

\item{incomplete_replicates}{Action for incomplete replicate information;
see \code{\link[=checkReplicateSeparation]{checkReplicateSeparation()}}.}

\item{panel_size}{Number of ranked candidates, or \code{Inf}.}

\item{reciprocal_only, unique_genes}{Candidate-ranking controls.}

\item{...}{Arguments passed to \code{\link[=makeStageDTU]{makeStageDTU()}} and \code{\link[=detectEpisodes]{detectEpisodes()}}.
Names shared by both functions are routed to both when appropriate.}
}
\value{
A validated \linkS4class{TransientDTUResult} object.
}
\description{
\code{runTransientDTU()} standardizes generic pairwise evidence, detects bounded
episodes, optionally enforces complete replicate separation, annotates
reciprocal exchanges, and creates a deterministic candidate ranking.
}
\examples{
simulated <- simulateTransientDTU(n_genes = 20, seed = 6)
result <- runTransientDTU(
    simulated$pairwise,
    stage_order = simulated$stage_order,
    usage = simulated$usage,
    col_data = simulated$col_data,
    panel_size = 6
)
result
candidateTable(result)

}
