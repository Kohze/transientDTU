% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability.R
\name{thresholdStability}
\alias{thresholdStability}
\title{Evaluate a joint decision-threshold grid}
\usage{
thresholdStability(
  stage_data,
  q_threshold = c(0.01, 0.05),
  effect_threshold = c(0.1, 0.15, 0.2),
  comparator_tolerance = effect_threshold,
  flank_tolerance = effect_threshold,
  gene_q_threshold = list(NULL),
  usage = NULL,
  col_data = NULL,
  panel_size = 6L,
  reciprocal_only = TRUE,
  min_comparators = 2L,
  episode_args = list(),
  replicate_action = c("none", "annotate", "filter"),
  ...
)
}
\arguments{
\item{stage_data}{Standardized stage evidence from \code{\link[=makeStageDTU]{makeStageDTU()}}.}

\item{q_threshold, effect_threshold, comparator_tolerance, flank_tolerance}{Numeric vectors defining a Cartesian threshold grid.}

\item{gene_q_threshold}{List or numeric vector of gene-level thresholds.
Include \code{NULL} in a list to evaluate no gene-level screen.}

\item{usage, col_data}{Optional replicate usage and metadata.}

\item{panel_size}{Candidate panel size.}

\item{reciprocal_only}{Restrict rankings to reciprocal groups.}

\item{min_comparators}{Minimum number of comparator groups.}

\item{episode_args}{Named fixed arguments passed to \code{\link[=detectEpisodes]{detectEpisodes()}}, such
as \code{flank_width}, \code{min_episode_stages}, or \code{stage_coordinates}. Grid
thresholds and \code{stage_data} cannot be overridden.}

\item{replicate_action}{\code{"none"}, \code{"annotate"}, or \code{"filter"}.}

\item{...}{Additional arguments passed to \code{\link[=checkReplicateSeparation]{checkReplicateSeparation()}}.}
}
\value{
A list containing \code{summary}, \code{selections}, and \code{selection_frequency}
\link[S4Vectors:DataFrame-class]{S4Vectors::DataFrame} objects plus the fixed \code{parameters}.
}
\description{
Re-runs the complete frozen detector across all joint combinations of the
principal thresholds and records episode counts and panel membership.
}
\examples{
simulated <- simulateTransientDTU(n_genes = 16, seed = 9)
stage <- makeStageDTU(simulated$pairwise, simulated$stage_order)
stability <- thresholdStability(
    stage,
    q_threshold = c(0.01, 0.05),
    effect_threshold = c(0.10, 0.15),
    usage = simulated$usage,
    col_data = simulated$col_data,
    replicate_action = "filter"
)
stability$selection_frequency

}
