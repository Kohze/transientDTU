% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeStageDTU.R
\name{makeStageDTU}
\alias{makeStageDTU}
\title{Standardize pairwise DTU evidence by focal group and stage}
\usage{
makeStageDTU(
  pairwise,
  stage_order,
  feature_col = "feature_id",
  gene_col = "gene_id",
  focal_col = "focal_group",
  comparator_col = "comparator_group",
  stage_col = "stage",
  effect_col = "effect",
  q_col = "q_value",
  gene_name_col = NULL,
  gene_q_col = NULL,
  expected_groups = NULL,
  min_comparators = 2L,
  require_all_comparators = TRUE
)
}
\arguments{
\item{pairwise}{A \code{data.frame} or \link[S4Vectors:DataFrame-class]{S4Vectors::DataFrame}.}

\item{stage_order}{Character vector giving the complete biological order of
stages. Values must be unique and must include every observed stage.}

\item{feature_col, gene_col, focal_col, comparator_col}{Column names defining
feature, gene, focal-group, and comparator-group identifiers.}

\item{stage_col, effect_col, q_col}{Column names defining stage, signed effect,
and adjusted component p-value values.}

\item{gene_name_col}{Optional display-name column.}

\item{gene_q_col}{Optional gene-level adjusted p-value column. Repeated
values within a feature, focal group, and stage are conservatively reduced
to their maximum.}

\item{expected_groups}{Optional complete set of biological groups. By
default it is inferred from the union of focal and comparator values.}

\item{min_comparators}{Minimum number of distinct comparison groups required
for a stage to be eligible. The default of two implements a focal group
separating from at least two alternatives.}

\item{require_all_comparators}{Whether every non-focal \code{expected_groups}
member must be observed. Recommended and \code{TRUE} by default.}
}
\value{
A \link[S4Vectors:DataFrame-class]{S4Vectors::DataFrame} with one row per observed feature, focal
group, and stage. It contains conservative component evidence, effect
summaries, comparator coherence, completeness, and the ordered stage
index.
}
\description{
\code{makeStageDTU()} converts a generic long pairwise contrast table into the
stage-level evidence used by \code{\link[=detectEpisodes]{detectEpisodes()}}. Upstream inference may come
from any method, provided each row supplies a signed focal-minus-comparator
usage effect and an adjusted p-value.
}
\details{
The comparator spread is \code{max(effect) - min(effect)}. Because all effects
use the same focal-minus-comparator orientation, this is exactly the range
of comparator-group means. Duplicate atomic contrasts are rejected rather
than silently reduced.
}
\examples{
simulated <- simulateTransientDTU(n_genes = 8, seed = 2)
stage <- makeStageDTU(simulated$pairwise, simulated$stage_order)
stage

}
