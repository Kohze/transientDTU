% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulateTransientDTU}
\alias{simulateTransientDTU}
\title{Simulate known transient and non-transient transcript-usage trajectories}
\usage{
simulateTransientDTU(
  n_genes = 100L,
  n_stages = 8L,
  groups = c("group1", "group2", "group3"),
  n_replicates = 2L,
  signal = 0.3,
  noise_sd = 0.015,
  transient_stage = ceiling(n_stages/2) + 1L,
  class_prob = rep(0.25, 4L),
  missing_fraction = 0,
  adjustment = "BH",
  seed = 1L
)
}
\arguments{
\item{n_genes}{Number of two-feature genes.}

\item{n_stages}{Number of ordered stages; at least five.}

\item{groups}{Character group identifiers; at least two. Two-group output
must be analyzed with \code{min_comparators = 1}.}

\item{n_replicates}{Replicates per group-stage cell.}

\item{signal}{Absolute usage shift for the first feature in affected genes.}

\item{noise_sd}{Gaussian replicate noise on the usage scale.}

\item{transient_stage}{Interior stage index for bounded signals.}

\item{class_prob}{Named or ordered probabilities for \code{null}, \code{monotonic},
\code{persistent}, and \code{transient} genes.}

\item{missing_fraction}{Fraction of atomic pairwise rows removed completely
at random after inference.}

\item{adjustment}{Multiplicity method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{seed}{Integer random seed.}
}
\value{
A list containing \code{pairwise}, \code{usage}, \code{col_data}, \code{truth},
\code{gene_truth}, \code{stage_order}, and \code{groups}.
}
\description{
Generates two-part compositional usage for genes assigned to null,
monotonic, persistent-divergence, or bounded transient trajectories. Pairwise
evidence uses focal-minus-comparator mean differences and normal-reference
p-values under the known simulation noise, followed by a global adjustment.
}
\details{
This simulation evaluates the post-inference decision layer. Its
normal-reference component p-values use the known generating noise and are
not intended to replace an RNA-seq count simulator or quantify transcript
assignment uncertainty.
}
\examples{
simulated <- simulateTransientDTU(n_genes = 20, seed = 11)
table(simulated$gene_truth$trajectory)
head(simulated$truth)

}
