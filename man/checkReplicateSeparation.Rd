% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replicates.R
\name{checkReplicateSeparation}
\alias{checkReplicateSeparation}
\title{Check complete replicate separation within detected episodes}
\usage{
checkReplicateSeparation(
  episodes,
  usage,
  col_data = NULL,
  assay_name = "usage",
  sample_col = NULL,
  group_col = "group",
  stage_col = "stage",
  min_separation = 0,
  replicate_method = c("complete", "quantile", "median"),
  quantile_probability = 0.1,
  missing_values = c("incomplete", "omit"),
  min_replicates = 1L,
  incomplete = c("keep", "drop", "error"),
  keep = c("all", "consistent")
)
}
\arguments{
\item{episodes}{Episode table returned by \code{\link[=detectEpisodes]{detectEpisodes()}}.}

\item{usage}{A numeric feature-by-sample matrix or a
\link[SummarizedExperiment:RangedSummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment} containing usage proportions.}

\item{col_data}{Sample metadata for matrix input. Row names, or \code{sample_col},
must match matrix columns.}

\item{assay_name}{Assay to use for \code{SummarizedExperiment} input.}

\item{sample_col}{Optional sample-identifier column in \code{col_data}.}

\item{group_col, stage_col}{Metadata columns defining groups and stages.}

\item{min_separation}{Strict lower bound for consistency. Zero requires
non-overlap in the episode direction.}

\item{replicate_method}{Summary used for each focal-versus-non-focal stage:
\code{"complete"} compares the most extreme replicates and reproduces the paper;
\code{"quantile"} compares inner quantiles; and \code{"median"} compares medians.
These are descriptive robustness checks, not replacement inference.}

\item{quantile_probability}{Tail probability for \code{replicate_method = "quantile"}; it must be strictly between zero and one half.}

\item{missing_values}{\code{"incomplete"} marks an episode incomplete when any
selected value is missing; \code{"omit"} removes non-finite values before
checking \code{min_replicates}.}

\item{min_replicates}{Minimum finite values required in the focal group and
in every expected comparator group at each episode stage.}

\item{incomplete}{One of \code{"keep"}, \code{"drop"}, or \code{"error"}, controlling
episodes lacking complete finite replicate values.}

\item{keep}{One of \code{"all"} or \code{"consistent"}.}
}
\value{
The episode \link[S4Vectors:DataFrame-class]{S4Vectors::DataFrame} with \code{replicate_separation} and
\code{replicate_consistent} updated.
}
\description{
Check complete replicate separation within detected episodes
}
\examples{
simulated <- simulateTransientDTU(n_genes = 12, seed = 5)
stage <- makeStageDTU(simulated$pairwise, simulated$stage_order)
episodes <- detectEpisodes(stage)
checked <- checkReplicateSeparation(
    episodes, simulated$usage, simulated$col_data
)
checked

}
