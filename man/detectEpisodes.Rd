% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detectEpisodes.R
\name{detectEpisodes}
\alias{detectEpisodes}
\title{Detect bounded diverge-reconverge episodes}
\usage{
detectEpisodes(
  stage_data,
  stage_order = NULL,
  q_threshold = 0.05,
  effect_threshold = 0.1,
  comparator_tolerance = effect_threshold,
  flank_tolerance = effect_threshold,
  gene_q_threshold = NULL,
  min_comparators = 2L,
  flank_missing = c("available", "complete")
)
}
\arguments{
\item{stage_data}{Output from \code{\link[=makeStageDTU]{makeStageDTU()}} or an equivalent table with
the documented columns.}

\item{stage_order}{Optional complete stage order. If omitted, the
\code{stage_order} attribute created by \code{\link[=makeStageDTU]{makeStageDTU()}} is used.}

\item{q_threshold}{Strict upper bound for every required component adjusted
p-value.}

\item{effect_threshold}{Inclusive lower bound for every absolute focal versus
comparator effect.}

\item{comparator_tolerance}{Strict upper bound for the range of comparator
means at an episode stage.}

\item{flank_tolerance}{Strict upper bound for every absolute focal versus
comparator effect at both immediate flanks.}

\item{gene_q_threshold}{Optional strict upper bound for gene-level adjusted
p-values. Use \code{NULL} to disable gene-level screening.}

\item{min_comparators}{Minimum number of comparator groups required.}

\item{flank_missing}{\code{"available"} reproduces the original rule by using
all finite immediate-flank effects and requiring at least one finite flank.
\code{"complete"} requires finite summaries at both flanks. Entirely absent
stage rows always break an episode under either setting.}
}
\value{
A \link[S4Vectors:DataFrame-class]{S4Vectors::DataFrame} with one row per detected feature-level
episode. Empty analyses return the same typed columns with zero rows.
}
\description{
Applies a frozen post-inference decision rule to standardized stage-level
evidence from \code{\link[=makeStageDTU]{makeStageDTU()}}. Consecutive eligible stages are collapsed
into a single episode and retained only when both immediate flanks satisfy
the reconvergence tolerance.
}
\details{
Missing stage rows are inserted as ineligible states and therefore break a
run. Runs touching the first or last stage are excluded because two observed
reconverged flanks cannot be demonstrated. Threshold strictness deliberately
matches the published decision rule: q-values, comparator spread, and flank
differences use \code{<}; episode effects use \code{>=}.
}
\examples{
simulated <- simulateTransientDTU(n_genes = 12, seed = 4)
stage <- makeStageDTU(simulated$pairwise, simulated$stage_order)
episodes <- detectEpisodes(stage)
episodes

}
