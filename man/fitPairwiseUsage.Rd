% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitPairwiseUsage.R
\name{fitPairwiseUsage}
\alias{fitPairwiseUsage}
\title{Fit stage-specific pairwise usage contrasts with limma}
\usage{
fitPairwiseUsage(
  usage,
  col_data = NULL,
  feature_data = NULL,
  assay_name = "usage",
  sample_col = NULL,
  feature_col = NULL,
  gene_col = "gene_id",
  gene_name_col = NULL,
  group_col = "group",
  stage_col = "stage",
  stage_order = NULL,
  adjustment = "BH",
  trend = TRUE,
  robust = TRUE
)
}
\arguments{
\item{usage}{A feature-by-sample proportion matrix or a
\link[SummarizedExperiment:RangedSummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment}.}

\item{col_data}{Sample metadata for matrix input.}

\item{feature_data}{Feature metadata for matrix input. Its row names must
match usage rows, unless \code{feature_col} identifies them explicitly.}

\item{assay_name}{Assay used for \code{SummarizedExperiment} input.}

\item{sample_col}{Optional sample identifier in \code{col_data}.}

\item{feature_col}{Optional feature identifier in \code{feature_data}.}

\item{gene_col}{Gene identifier in feature metadata.}

\item{gene_name_col}{Optional gene display-name column.}

\item{group_col, stage_col}{Sample metadata columns.}

\item{stage_order}{Complete ordered stages. Inferred from a factor's levels
or first appearance when omitted; supplying it explicitly is recommended.}

\item{adjustment}{Adjustment method applied across the complete directed
feature-by-stage-by-pair family.}

\item{trend, robust}{Arguments passed to \code{\link[limma:ebayes]{limma::eBayes()}}.}
}
\value{
A base \code{data.frame} satisfying the default \code{\link[=makeStageDTU]{makeStageDTU()}} schema.
}
\description{
Convenience inference adapter for users starting from replicate-level usage
proportions. It fits a saturated group-by-stage cell-means model with
\code{\link[limma:lmFit]{limma::lmFit()}}, evaluates every directed focal-versus-comparator contrast
within each stage, and returns the generic table accepted by
\code{\link[=makeStageDTU]{makeStageDTU()}}. The detector itself remains independent of limma.
}
\details{
Gene-level p-values are Simes combinations of all raw component p-values for
a gene and are then adjusted across genes. They are provided as an optional
screen, not as episode-level error control. Direct proportion-scale limma is
a convenience adapter; users with a preferred DTU model should supply its
results directly to \code{\link[=makeStageDTU]{makeStageDTU()}}.
}
\examples{
if (requireNamespace("limma", quietly = TRUE)) {
    simulated <- simulateTransientDTU(n_genes = 8, seed = 12)
    pairwise <- fitPairwiseUsage(
        simulated$usage,
        simulated$col_data,
        feature_data = data.frame(
            gene_id = sub("_tx[12]$", "", rownames(simulated$usage)),
            row.names = rownames(simulated$usage)
        ),
        stage_order = simulated$stage_order
    )
    head(pairwise)
}

}
