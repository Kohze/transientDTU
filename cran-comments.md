## Release summary

This is the initial CRAN submission of transientDTU.

## R CMD check results

Local Windows 11, R 4.4.0:

- 0 ERRORs
- 0 WARNINGs
- 1 NOTE

The NOTE states that the current time could not be verified because the local
sandbox has no outbound network access. CRAN incoming checks were unavailable
for the same reason. This is not a package-generated condition.

Before upload, replace or supplement this local result with current R-devel,
R-release, and old-release results. The source tarball must receive no ERRORs,
WARNINGs, or unexplained NOTEs on those environments.

## Downstream dependencies

There are no downstream dependencies because this is a new submission.
